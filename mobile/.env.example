# Backend API URL
# Your backend service that generates signed URLs for Google Cloud Storage
BACKEND_URL=http://localhost:3000

# Note: GCS credentials should ONLY be on your backend server
# Never expose service account keys or credentials in your mobile app
